# Copy to .env.local in this folder (Create React App picks it up on restart):
#   cp env.example .env.local
#
# Backend must listen on this host/port (e.g. uvicorn on 8000).
REACT_APP_BACKEND_URL=http://127.0.0.1:8000
